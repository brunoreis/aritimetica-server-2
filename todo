
- Create user roles: "ADMIN", "TEACHER", "STUDENT"
    - Prisma and seeds
    - type-graphql objects
    - test with allUsers queries
    - inject roles into the TOKEN on login
    - use @Authorized middleware 
    - allow ADMINs only to see the allUsers resolver
- deploy 
    - create local docker machines to run it
        - postgress db
            - migrate password fields
        - postgress tool to see the db
        - node 
    - deploy
        - add restrictions
