version: "3"

services:
  db:
    image: postgres
    volumes:
      - postgres:/var/lib/postgresql/data/
    environment:
      POSTGRES_USER: gepeto
      POSTGRES_PASSWORD: pin0quio
      POSTGRES_DB: gepeto_server
    ports:
      - "5432:5432"
volumes:
  postgres:
